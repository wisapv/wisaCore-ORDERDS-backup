# PostgreSQL connection string used by the Min-Max calculation history feature.
# Install PostgreSQL locally and create a database (e.g. `createdb wisacore_minmax`),
# then copy this file to .env and adjust as needed.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/wisacore_minmax
